{
  "name": "gourmet-pos",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --root frontend",
    "prebuild": "npm install --include=dev",
    "build": "cd frontend && vite build --outDir dist",
    "preview": "vite preview --root frontend --port 5173"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.35",
    "tailwindcss": "^4.0.0",
    "vite": "^5.0.0"
  }
}
